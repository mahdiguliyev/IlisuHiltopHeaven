@model IlisuHiltopHeaven.Presentation.Areas.Admin.Models.AdvantageAddViewModel
@{
    Layout = "_Layout";
    ViewBag.Title = "Avantaj Əlavə Et";
}


<div class="row">
    <div class="col-lg-8 offset-lg-2">
        <div class="card mt-1 mb-1">
            <div class="card-header">
                <h4 class="text-center"><span class="fas fa-edit"></span> Avantaj əlavə et </h4>
            </div>
            <div class="card-body">
                <form asp-area="admin" asp-controller="advantage" asp-action="add" method="post" id="form-generalsettings-update">
                    <div class="form-row">
                        <div class="form-group col-md-12">
                            <label asp-for="Title"></label>
                            <input asp-for="Title" type="text" class="form-control" />
                            <span asp-validation-for="Title" class="text-danger"></span>
                        </div>
                        <div class="form-group col-md-12">
                            <label asp-for="LanguageId" for="languageList"></label>
                            <select asp-for="LanguageId" asp-items="@(new SelectList(Model.Languages, "Id", "LanguageCode"))" id="languageList" class="form-control">
                                <option></option>
                            </select>
                            <span asp-validation-for="LanguageId" class="text-danger"></span>
                        </div>
                        <div class="form-group col-md-12">
                            <button type="submit" class="btn btn-primary"><span class="fas fa-save"></span> Yadda saxla</button>
                            <a class="btn btn-danger" asp-area="admin" asp-controller="advantage" asp-action="index"><span class="fas fa-times"></span> Geri</a>
                        </div>
                    </div>

                </form>
            </div>
        </div>
    </div>
</div>